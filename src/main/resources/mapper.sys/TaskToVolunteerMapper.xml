<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sxt.sys.mapper.TaskToVolunteerMapper">
    <select id="selectByOpenIdAndTaskId" resultType="com.sxt.sys.domain.TaskToVolunteer">

    SELECT * from  task_volunteer where open_id=#{openId} and task_id=#{taskId}
    </select>
</mapper>
